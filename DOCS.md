# 要件定義・技術選定

関西ビギナーズハッカソン vol.8 ／ 2026-08-29 時点
デモURL: https://kyoto-repeater.vercel.app/
リポジトリ: https://github.com/supermonkey0623-commits/kyoto-repeater

---

# 第1部　要件定義

## 1. 誰の、何を解くか

| | 内容 |
|---|---|
| **ターゲット** | 京都に複数回訪れたリピーター観光客 |
| **課題** | 定番は行き尽くした。次の行き先が思いつかない |
| **原因** | 検索・地図・レビュー・生成AIは、すべて「人気順」で返す |
| **帰結** | 上位群を消費済みのリピーターには、既存手段が構造的に無効 |

**根拠**：京都市は「リピーターは主要観光地以外を訪れる傾向があり、リピーター確保が観光需要の分散に寄与する」としている（京都観光に関する市民意識調査／観光客の分散化）。

## 2. コンセプト

> **場所を伏せたまま写真で出会わせ、ポイントを払うと正確な場所が分かる。**

「人気順の逆」を、UIそのもので実装する。

## 3. 機能要件（実装済み）

| # | 機能 | 内容 |
|---|---|---|
| F-1 | サムネ一覧 | 写真・タイトルのみ。**場所も人気の数字も出さない** |
| F-2 | かんたん検索 | キーワード＋6条件で、条件に合う順に提案 |
| F-3 | 投稿詳細（ペイウォール） | 1pt を払うと**正確な場所＋Googleマップリンク**と本文が開く |
| F-4 | ポイント経済 | 初期10pt／閲覧 −1pt／「✨新しい」で +1pt |
| F-5 | 投稿 | カメラ／ライブラリ選択、**写真のEXIFから撮影地点を自動取得** |
| F-6 | プロフィール | 残高・投稿数・読んだ数・送った✨・もらった✨／自分の投稿一覧 |
| F-7 | スワイプ操作 | 左右スワイプでタブ切り替え |
| F-8 | 公開 | 認証なしの公開URL／GitHub public |

### F-2 の条件（6軸）

キーワード ／ 気分・趣味 ／ 誰と ／ 天気 ／ 時間帯 ／ 空き時間 ／ 予算

**AND検索にしていない。** 母数が小さい段階でANDにすると2条件で0件になるため、条件に合うほど加点して並べ替える。**必ず上位5件が出る。** 「気分に合う」「雨でも大丈夫」など、選ばれた理由を各件に表示する。

## 4. 非機能要件

| 項目 | 要件 | 実測・実装 |
|---|---|---|
| 対応環境 | スマートフォンのブラウザ | iOS Safari / Android Chrome |
| 表示速度 | 会場回線で3秒以内 | 全ページ静的生成、110〜128 kB |
| 認証 | なし | 匿名。QRから即操作可能 |
| 同時接続 | 発表時30〜50名 | 静的配信のため実質無制限 |
| 障害対策 | 回線断でもデモ継続 | デモ録画を用意 |

## 5. スコープ外（意図的に捨てたもの）

| 捨てたもの | 理由 |
|---|---|
| ログイン・認証 | 審査要件「誰でも触れる」を自ら壊すため |
| いいね数・人気順の表示 | 人気順の否定がコンセプトの前提 |
| AIによる提案 | 実装実態と乖離する。抽出は決定的なスコア計算のみ |
| 収益モデルの提示 | 店舗課金は「支払い順」となり、無名順の思想と矛盾する |
| 経路提案・現在地からの距離 | 座標は取得済み。時間の制約で今回は見送り |

---

# 第2部　技術選定

## 1. 構成

| 層 | 採用 | 選定理由 |
|---|---|---|
| フロントエンド | **Next.js 15**（App Router）+ React 19 + TypeScript | 全ページ静的生成でき、会場回線でも確実に開く |
| ホスティング | **Vercel** | Git push で自動デプロイ。公開URLが即出る |
| データ取得 | **Google Places API (New)** | 京都市内のスポットを機械的に収集 |
| 状態の保存 | **localStorage** | サーバー・認証なしで体験を成立させる |
| 位置情報 | **自作EXIFパーサ** + Geolocation API | 写真から撮影地点を取得。外部ライブラリ不要 |
| スタイル | **プレーンCSS** | UIライブラリ不使用。転送量を抑える |

**外部依存は 60 パッケージのみ**（Next.js/React本体を含む）。UIライブラリ・状態管理・EXIFライブラリをいずれも入れていない。

## 2. 判断軸

| 軸 | 判断 |
|---|---|
| **確実に動くこと** | 会場のWi-Fiで開かなければ0点。静的生成・軽量・外部依存最小 |
| **審査要件を壊さないこと** | 認証を入れない。QRから1タップで全機能に触れる |
| **説明できること** | 提案ロジックはAIを使わない加点処理。動作を1枚で説明できる |

## 3. データの作り方

```
Places API (New)  →  1,370件取得
        ↓ 機械フィルタ（評価4.0以上／口コミ5〜49件／業種・チェーン除外）
      291件
        ↓ 人の目による選別
   掲載する投稿
```

**機械だけでは決められない。** 口コミが少ない場所を機械的に並べると、デパ地下の菓子カウンターが上位に出る。「口コミが少ない＝穴場」は成立しないため、最後は人が選ぶ設計にした。

## 4. 採用しなかった技術

| 不採用 | 理由 |
|---|---|
| Supabase等のDB | 認証とセットになる。今回の体験には不要と判断 |
| NextAuth等の認証 | 「誰でも触れる」要件と直接衝突する |
| Instagram Graph API | ハッシュタグ検索はApp Review必須（数日〜数週間）。位置情報も返らない |
| EXIF読み取りライブラリ | 必要なGPSタグのみ自前実装。依存を増やさない |
| next/image | 事前に縮小した画像を配信。最適化サーバーを挟まないほうが速い |

## 5. 現状の制約（正直な記載）

| 制約 | 内容 |
|---|---|
| 端末ローカル | ポイント・投稿は localStorage。端末を跨がない |
| 反応が届かない | 他ユーザーからの「✨新しい」は、アカウントが無いため未接続 |
| EXIFの欠落 | SNS経由の画像はEXIFが削除される。現在地取得で代替 |

---

## 今後

**短期**：投稿と反応をサーバーへ／端末を跨いだポイント
**中期**：消費ログの蓄積による提案精度の向上／現在地からの距離・経路
**長期**：マップ型UI／京都以外の都市への展開
